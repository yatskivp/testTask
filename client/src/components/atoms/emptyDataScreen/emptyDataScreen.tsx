import { Typography } from '@mui/material';

export const EmptyDataScreen = () => {
  return (
    <Typography variant="h6" sx={{ textAlign: 'center' }}>
      No data to display
    </Typography>
  );
};
